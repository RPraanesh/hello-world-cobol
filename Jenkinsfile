pipeline {
   agent any
      stages {
      stage ('Build') {
      steps {
      echo 'Running Build Automation '
      sh '/home/centos/cobol/hello-world-cobol/build.sh --no-daemon'
                       }
            }
       }
}
